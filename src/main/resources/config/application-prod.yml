spring:
  application:
    name: spring-boot-cometd
  messages:
    encoding: UTF-8

server:
  port: 9011
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
    context-path: /
  tomcat:
    uri-encoding: UTF-8
    basedir: ../tomcat/${server.port}